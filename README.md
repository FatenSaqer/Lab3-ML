This Data Analysis (EDA) examines a cybersecurity intrusion dataset to understand patterns in network session data and identify features that may help detect malicious activity.

Each row in the dataset represents a network session, and the target variable attack_detected indicates whether the session is normal or an intrusion.
